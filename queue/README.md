# Queue

Operator pre-approved work packages for the production run (Phase 0 reads
`queue/*.md`). Drop a dated or date-less `.md` file here describing a one-shot
instruction set, its scope of autonomy, and post-execution cleanup. The run
deletes the file once executed (with approval, per hard rules).

Formerly `~/Documents/Claude/Scheduled/logiai-production-stack/queue/` on the
Mac. Empty is the normal state.
